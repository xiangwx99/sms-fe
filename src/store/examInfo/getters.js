const getters = {
  getChoice(state) {
    return state.choice;
  },
  getJudge(state) {
    return state.judge;
  },
  getFillBlank(state) {
    return state.fillBlank;
  },
  getJudge(state) {
    return state.briefAnswer;
  },
};

export default getters;
